specVersion: "v1"
serviceId: "notes-website"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "notes-website-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
